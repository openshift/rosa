#!/usr/bin/env bash
# Copyright Red Hat
# SPDX-License-Identifier: Apache-2.0

set -euo pipefail

echo "Push blocked: this clone uses the legacy .githooks path. Run 'make install-hooks' to migrate to pre-commit hooks." >&2
exit 1
